image: docker:24.0
services:
  - docker:24-dind

stages:
  - build
  - test
  - release
  - deploy

variables:
  CONTAINER_TEST_IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG
  CONTAINER_RELEASE_IMAGE: $CI_REGISTRY_IMAGE:latest

before_script:
  - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY

build-backend:
  stage: build
  script:
    - docker build -t $CONTAINER_TEST_IMAGE ./server/
    - docker push $CONTAINER_TEST_IMAGE

linter-test-backend:
  stage: test
  script:
    - docker pull $CONTAINER_TEST_IMAGE
    - docker run $CONTAINER_TEST_IMAGE npm ci
    - docker run $CONTAINER_TEST_IMAGE npm run lint

unit-test-backend:
  stage: test
  script:
    - docker pull $CONTAINER_TEST_IMAGE
    - docker run $CONTAINER_TEST_IMAGE npm run test:unit

release-image-backend:
  stage: release
  script:
    - docker pull $CONTAINER_TEST_IMAGE
    - docker tag $CONTAINER_TEST_IMAGE $CONTAINER_RELEASE_IMAGE
    - docker push $CONTAINER_RELEASE_IMAGE

deploy-backend:
  stage: deploy
  tags:
    - local_docker
  script:
    - docker pull $CONTAINER_TEST_IMAGE ./server/
    - docker run -d -p 4001:4001 $CONTAINER_RELEASE_IMAGE
