stages:
  - build
  - test

build:
  stage: build
  image: node:16-alpine
  script:
    - echo "Start building App"
    - npm install
    - npm build
    - echo "Build successfully!"

test:
  stage: test
  image: node:16-alpine
  script:
    - echo "Testing App"
    - npm install
    - CI=true npm test
    - echo "Test successfully!"
